# Core demo: zero third-party dependencies (uses only the Python stdlib).
# The packages below are required ONLY when running the optional Databricks
# pipeline via `py scripts\generate_sidley_pbip.py --databricks`.
# `--dry-run` works without them.
databricks-sdk>=0.30,<1.0
databricks-sql-connector>=4.2.6,<5.0
