# Sample Output Note

Run this to generate the actual PBIP project locally:

```powershell
py scripts\generate_sidley_pbip.py
```

The generator writes absolute CSV file paths into `model.bim` so Power BI Desktop can load the generated local files. That is why the full generated output is not pre-baked in this zip.
